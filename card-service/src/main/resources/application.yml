server:
  port: 0
spring:
  application:
    name: card-service
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5306/cardsdb
  jpa:
    hibernate:
      ddl-auto: validate
